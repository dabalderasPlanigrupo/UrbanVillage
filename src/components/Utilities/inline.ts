import styled from 'styled-components';

const Inline = styled.div`
  align-items: center;
  text-align: center;
  z-index: 1;
  .Select-menu-outer {
    z-index: 999 !important;
  }
`;

export { Inline };
